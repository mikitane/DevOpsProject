image: docker/compose:latest

services:
  - docker:dind
# before_script:
#   - curl -L "https://github.com/docker/compose/releases/download/1.27.4/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
#   - chmod +x /usr/local/bin/docker-compose
#   - ln -s /usr/local/bin/docker-compose /usr/bin/docker-compose

build:


  stage: build
  script:
    - docker build ./tests/Dockerfile -t testimage:latest
    - docker-compose -f docker-compose.test.yml up --build -d

test:
  stage: test
  script:
    - docker run --network="testing_network" testimage:latest python healthcheck.py
    - docker run --network="testing_network" devopsproject_test_service python test_httpserv.py
    - docker run --network="testing_network" devopsproject_test_service python test_apigateway.py
    - docker-compose -f docker-compose.test.yml stop

deploy:
  stage: deploy
  script:
    - docker-compose up --build -d

